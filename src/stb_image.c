/* stb_image implementation translation unit.
 *
 * stb_image is public domain (https://github.com/nothings/stb).
 * The decoder is defined exactly once, here; every other TU includes
 * "stb_image.h" without STB_IMAGE_IMPLEMENTATION. */
#define STB_IMAGE_IMPLEMENTATION
#include "stb_image.h"
